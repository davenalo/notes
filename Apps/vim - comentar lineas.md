Utiliza el modo visual block:

ctrl+v

seleccionar las lineas

Insertar al principio de la línea (shift+i)

incluir el caracter (#, por ejemplo), que solo aparecerá en la primera línea hasta que se pulse ESC.

Para descomentar:

ctrl+v

seleccionar líneas

borrar caracter (x)